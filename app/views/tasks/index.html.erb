<h1>These are your tasks cutie!</h1>

<ol>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> (completed: <%= task.completed %>) </li>
  <% end %>
</ol>

<%= link_to "Create a new task", new_path  %>
